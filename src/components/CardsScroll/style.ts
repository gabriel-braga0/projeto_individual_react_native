import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  titleCards: {
    paddingTop: 35,
    fontSize: 22,
    fontWeight: "bold",
    color: "white",
    paddingHorizontal: 20,
  },
  cardContainer: {
    flexDirection: "row",
    paddingHorizontal: 20,
    paddingTop: 20,
  },
});
